# Pfun V2 baseline inventory

Generated by `node scripts/baseline-inventory.mjs` (Completion Specification S0 task 5). Derived from tracked source only, so repeated runs on the same tree are byte-identical. Machine-specific identity lives in `baseline/environment.md`.

Source files scanned: 72

## 1. Permissive types

§4.1 makes `TAny` and `TUnknown` non-evidence: neither may bind or ground a normal field, discharge a class requirement, or appear in a finalized export. Every code occurrence below is a site S1 must classify as *removed*, *quarantined to the native boundary*, or *recovery taint that must not cascade*.

### `TAny`

Code occurrences: **25** (plus 0 in comments).

Reachable from ordinary user code today via the four builtin signatures in section 3.

| File | Occurrences |
|---|---|
| `src/check/exhaust.pf` | 1 |
| `src/check/types.pf` | 24 |

```text
src/check/exhaust.pf:240: | TAny -> SubjectUnknown
src/check/types.pf:30: | TAny
src/check/types.pf:140: export function tAny() { TAny }
src/check/types.pf:337: | TAny -> "Any"
src/check/types.pf:540: | TAny -> true
src/check/types.pf:593: else if nm == "Any" then { TAny }
src/check/types.pf:820: | TAny -> st
src/check/types.pf:827: | TAny -> st
src/check/types.pf:835: | TAny -> st
src/check/types.pf:846: | TAny -> st
src/check/types.pf:857: | TAny -> st
src/check/types.pf:870: | TAny -> st
src/check/types.pf:882: | TAny -> st
src/check/types.pf:919: | TAny -> st
src/check/types.pf:926: | TAny -> st
src/check/types.pf:984: | TAny -> true
src/check/types.pf:1022: | TAny -> st
src/check/types.pf:1127: | TAny -> ITAny
src/check/types.pf:1151: | ITAny -> TAny
src/check/types.pf:1945: | TAny -> false
src/check/types.pf:1954: | TAny -> false
src/check/types.pf:2057: | TAny -> false
src/check/types.pf:2300: | TAny -> exprOut(co.st, TUnknown)
src/check/types.pf:2376: | TAny -> exprOut(st, TUnknown)
src/check/types.pf:2924: | TAny -> exprOut(st, TUnknown)
```

### `ITAny`

Code occurrences: **3** (plus 0 in comments).

Interface counterpart; §4.1 rule 7 applies the same rules.

| File | Occurrences |
|---|---|
| `src/check/iface.pf` | 1 |
| `src/check/types.pf` | 2 |

```text
src/check/iface.pf:40: | ITAny
src/check/types.pf:1127: | TAny -> ITAny
src/check/types.pf:1151: | ITAny -> TAny
```

### `TUnknown`

Code occurrences: **77** (plus 1 in comments).

Recovery taint rather than a dynamic type. §4.1 rule 5: taints a field without binding it, and must not emit a second diagnostic when it originated from an earlier failure.

| File | Occurrences |
|---|---|
| `src/check/exhaust.pf` | 1 |
| `src/check/types.pf` | 71 |
| `src/compile/emit.pf` | 5 |

```text
src/check/exhaust.pf:239: | TUnknown -> SubjectUnknown
src/check/types.pf:45: | TUnknown
src/check/types.pf:149: export function tUnknown() { TUnknown }
src/check/types.pf:352: | TUnknown -> "?"
src/check/types.pf:541: | TUnknown -> true
src/check/types.pf:594: else if nm == "List" then { TList { maybeFirst(argTs, TUnknown) } }
src/check/types.pf:595: else if nm == "Array" then { TArray { maybeFirst(argTs, TUnknown) } }
src/check/types.pf:685: | None -> TUnknown
src/check/types.pf:821: | TUnknown -> st
src/check/types.pf:828: | TUnknown -> st
src/check/types.pf:836: | TUnknown -> st
src/check/types.pf:847: | TUnknown -> st
src/check/types.pf:858: | TUnknown -> st
src/check/types.pf:871: | TUnknown -> st
src/check/types.pf:883: | TUnknown -> st
src/check/types.pf:920: | TUnknown -> st
src/check/types.pf:927: | TUnknown -> st
src/check/types.pf:983: | TUnknown -> true
src/check/types.pf:1021: | TUnknown -> st
src/check/types.pf:1136: | TUnknown -> ITUnknown
src/check/types.pf:1141: | ITUnknown -> TUnknown
src/check/types.pf:1759: | None -> TUnknown
src/check/types.pf:1762: | FieldCommon _ -> TUnknown
src/check/types.pf:1834: let env3 = IMS.imsPut(env2, "None", TMono { tOption(TUnknown) });
src/check/types.pf:1883: | Some kind -> putEnv(env, local, TMono { TUnknown })
src/check/types.pf:1944: | TUnknown -> false
src/check/types.pf:1953: | TUnknown -> false
src/check/types.pf:1990: exprOut(addTypeDiag(st, path, span, "Operator '" ++ op ++ "' requires a numeric operand."), TUnknown)
src/check/types.pf:2056: | TUnknown -> false
src/check/types.pf:2115: | TUnknown -> exprOut(st1, TUnknown)
src/check/types.pf:2151: TUnknown
src/check/types.pf:2221: TUnknown
src/check/types.pf:2244: TUnknown
src/check/types.pf:2299: | TUnknown -> exprOut(co.st, TUnknown)
src/check/types.pf:2300: | TAny -> exprOut(co.st, TUnknown)
src/check/types.pf:2301: | _ -> exprOut(addTypeDiag(co.st, path, span, "Cannot call value of type " ++ formatType(ct) ++ "."), TUnknown)
src/check/types.pf:2336: TUnknown
src/check/types.pf:2340: | None -> exprOut(st, TUnknown)
src/check/types.pf:2375: | TUnknown -> exprOut(st, TUnknown)
src/check/types.pf:2376: | TAny -> exprOut(st, TUnknown)
src/check/types.pf:2384: TUnknown
src/check/types.pf:2393: let valueT = maybeFirst(args.value, TUnknown);
src/check/types.pf:2414: let valueT = maybeFirst(leftArgs.value, TUnknown);
src/check/types.pf:2416: | None -> TUnknown
src/check/types.pf:2433: let resultT = maybeFirst(rightArgs.value, TUnknown);
src/check/types.pf:2435: | None -> TUnknown
src/check/types.pf:2541: | None -> exprOut(st, TUnknown)
src/check/types.pf:2595: TUnknown
src/check/types.pf:2616: TUnknown
src/check/types.pf:2796: | None -> exprOut(addTypeDiag(st, path, span, "Unknown record or variant constructor '" ++ name ++ "'."), TUnknown)
src/check/types.pf:2826: TUnknown
src/check/types.pf:2846: TUnknown
src/check/types.pf:2875: TUnknown
src/check/types.pf:2895: TUnknown
src/check/types.pf:2923: | TUnknown -> exprOut(st, TUnknown)
src/check/types.pf:2924: | TAny -> exprOut(st, TUnknown)
src/check/types.pf:2937: TUnknown
src/check/types.pf:2954: | _ -> exprOut(addTypeDiag(st, path, span, "Cannot index value of type " ++ formatType(t) ++ "."), TUnknown)
src/check/types.pf:3175: TUnknown,
src/check/types.pf:3230: | TUnknown -> constrainUnknownMatchSubject(
src/check/types.pf:3273: | None -> exprOut(addTypedNode(addTypeDiag(st, path, x.span, "Unknown name '" ++ x.name ++ "'."), x.id, TUnknown), TUnknown)
src/check/types.pf:3287: exprOut(addTypedNode(addTypeDiag(st, path, x.span, "Variant '" ++ x.name ++ "' requires a payload."), x.id, TUnknown), TUnknown)
src/check/types.pf:3290: | TNamespace _ -> exprOut(addTypedNode(st, x.id, TUnknown), TUnknown)
src/check/types.pf:3291: | TTypeName _ -> exprOut(addTypedNode(st, x.id, TUnknown), TUnknown)
src/check/types.pf:3390: | _ -> exprOut(addTypedNode(addTypeDiag(st, path, x.span, "Namespace member '" ++ x.fname ++ "' is not a value."), x.id, TUnknown), TUnknown)
src/check/types.pf:3392: | NsMissing ms -> exprOut(addTypedNode(addTypeDiag(st, path, x.span, "Namespace '" ++ ms.alias ++ "' has no export '" ++ x.fname ++ "'."), x.id, TUnknown), TUnknown)
src/check/types.pf:4633: | None -> TUnknown
src/check/types.pf:4641: | _ -> TUnknown
src/check/types.pf:5025: let target = match e.value with | TMono m -> m.t | TScheme sc -> sc.scheme.body | _ -> TUnknown;
src/check/types.pf:5037: | TNamed opt -> { match Compat.listAt(opt.args, 0) with | None -> TUnknown | Some v -> v.value }
src/check/types.pf:5038: | _ -> TUnknown;
src/check/types.pf:5239: mkScheme([], [], TUnknown)
src/compile/emit.pf:842: | _ -> TUnknown
src/compile/emit.pf:976: | None -> TUnknown
src/compile/emit.pf:980: | Some _ -> TUnknown
src/compile/emit.pf:983: TUnknown
src/compile/emit.pf:986: | _ -> TUnknown
```

## 2. Field shape representation

F1 task 4 replaces the type-bearing `FieldMono` with a key-bearing reference, and F2/F3 remove the `FieldCommon` path that currently answers `TUnknown` for a union-wide common-field access.

### `FieldMono`

Code occurrences: **5** (plus 1 in comments).

| File | Occurrences |
|---|---|
| `src/check/types.pf` | 5 |

```text
src/check/types.pf:87: | FieldMono: t
src/check/types.pf:458: | FieldMono f -> {
src/check/types.pf:1167: function fieldMono(t) { FieldMono { t } }
src/check/types.pf:1495: | FieldMono _ -> A.mkFieldDecl(field.fname, false)
src/check/types.pf:1756: | FieldMono f -> f.t
```

### `FieldCommon`

Code occurrences: **6** (plus 0 in comments).

| File | Occurrences |
|---|---|
| `src/check/types.pf` | 6 |

```text
src/check/types.pf:92: | FieldCommon: shapes
src/check/types.pf:463: | FieldCommon c -> fieldShapesFreeVars(st, ownName, c.shapes, [])
src/check/types.pf:1169: function fieldCommon(shapes) { FieldCommon { shapes } }
src/check/types.pf:1497: | FieldCommon _ -> A.mkFieldDecl(field.fname, false)
src/check/types.pf:1762: | FieldCommon _ -> TUnknown
src/check/types.pf:1784: | FieldCommon common -> {
```

## 3. Builtin signatures using `Any`

The S1 task 4 worklist. §4.2 replaces each with type-directed cases.

```text
src/builtins/spec.pf:265: pure("length", sch([], fn1(TY.tAny(), TY.tInt())), "$length"),
src/builtins/spec.pf:270: pure("slice", sch([], fn3(TY.tInt(), TY.tInt(), TY.tAny(), TY.tAny())), "$slice"),
src/builtins/spec.pf:273: pure("find", sch([], fn2(TY.tAny(), TY.tAny(), optionOf(TY.tInt()))), "$find"),
src/builtins/spec.pf:274: pure("findSlice", sch([], fn2(TY.tAny(), TY.tAny(), optionOf(TY.tInt()))), "$findSlice"),
```

## 4. Unsafe intrinsics

§6.24 keeps `$nthU` and `$chrU` as compiler-proved lowering intrinsics but removes them as ambient user APIs. Each Pfun-level call site below must move to a trusted compiler-only namespace or to an explicit verified import.

### `nthU` / `$nthU`

Pfun-level occurrences: **13**. Host-level occurrences: **4**.

```text
examples/example-v2/pipelines.pf:46: let first = nthU(rows, 0);
examples/example-v2/pipelines.pf:47: let second = nthU(rows, 1);
examples/example-v2/pipelines.pf:53: [nthU(first, 0), nthU(second, 0)],
examples/example-v2/pipelines.pf:54: [nthU(first, 1), nthU(second, 1)]
src/builtins/spec.pf:269: pure("nthU", sch([0], fn2(listOf(a), TY.tInt(), a)), "$nthU"),
src/compat.pf:8: else { Some { nthU(xs, i) } }
src/compat.pf:16: else { Some { Pair { nthU(xs, 0), slice(1, length(xs) - 1, xs) } } }
test/parser_test.pf:56: && nthU(union.includes, 0) == "FileError"
test/parser_test.pf:57: && nthU(union.includes, 1) == "JsonError"
test/parser_test_gen.pf:58: && nthU(union.includes, 0) == "FileError"
test/parser_test_gen.pf:59: && nthU(union.includes, 1) == "JsonError"
test/types_test.pf:489: let src = "let n = nthU([1, 2], 0) + 1;\nlet c = __str__(chrU(13));\nlet o = nth([1, 2], 0);";
test/types_test_gen.pf:491: let src = "let n = nthU([1, 2], 0) + 1;\nlet c = __str__(chrU(13));\nlet o = nth([1, 2], 0);";
host/browser.js:92: nthU: core.$nthU,
host/core.js:994: function $nthU(xs, index) {
host/core.js:1878: $nthU,
host/node.js:894: nthU: core.$nthU,
```

### `chrU` / `$chrU`

Pfun-level occurrences: **13**. Host-level occurrences: **4**.

```text
src/builtins/spec.pf:283: pure("chrU", sch([], fn1(TY.tInt(), TY.tChar())), "$chrU"),
src/compile/js.pf:373: } else if c == __str__(chrU(code)) then {
src/compile/js.pf:444: } else if c == __str__(chrU(127)) then {
src/compile/js.pf:460: } else if c == __str__(chrU(13)) then {
src/compile/js.pf:462: } else if c == __str__(chrU(8)) then {
src/compile/js.pf:464: } else if c == __str__(chrU(12)) then {
src/compile/js.pf:470: } else if c == __str__(chrU(8232)) then {
src/compile/js.pf:472: } else if c == __str__(chrU(8233)) then {
src/syntax/lexer.pf:134: c == " " || c == "\t" || c == "\n" || c == __str__(chrU(13))
test/lexer_test.pf:178: let cr = __str__(chrU(13));
test/lexer_test_gen.pf:180: let cr = __str__(chrU(13));
test/types_test.pf:489: let src = "let n = nthU([1, 2], 0) + 1;\nlet c = __str__(chrU(13));\nlet o = nth([1, 2], 0);";
test/types_test_gen.pf:491: let src = "let n = nthU([1, 2], 0) + 1;\nlet c = __str__(chrU(13));\nlet o = nth([1, 2], 0);";
host/browser.js:105: chrU: core.$chrU,
host/core.js:379: function $chrU(n) {
host/core.js:1953: $chr, $chrU,
host/node.js:907: chrU: core.$chrU,
```

## 5. Host throw sites (`$runtimeError`)

Every one of these raises a host exception in place of a typed failure. S1/S2 and the N phases decide per site whether it becomes a contract violation, a `NativeError`, or a statically excluded case.

Total: **63**

| File | Sites |
|---|---|
| `host/core.js` | 63 |

Distinct messages:

```text
(non-literal message)
++ expects Str and Str
append expects Array
appendBuffer expects Buffer
arrGet expects Array
arrSet expects Array
arrayLength expects Array
asc expects Char
bufferLength expects Buffer
bufferToBytes expects Buffer
bufferToString expects Buffer
cons expects List or Str
cons(Char, Str) expects Char
dict get expects Dict
dict set expects Dict
dictToList expects Dict
dictionary entry must be [key, value]
expected List
expected NativeError
field '
field access on null
filter expects List or Str
has expects Dict
index assignment expects Array or Dict
index expects Str, List, Array, or Dict
insertAt expects Array
integer division by zero reached host runtime
integer modulo by zero reached host runtime
internal list-pattern index is out of bounds
invalid emitted JSON schema
keys expects Dict
lazyList element is not a thunk
lazyList expects an array of thunks
length expects Str or List
list pattern expects List
listRest expects List
map expects List or Str
no UTF-8 decoder available
no UTF-8 encoder available
registerSchemas expects a schema array
remove expects Dict
removeAt expects Array
slice expects Str or List
strAt expects Str
tail expects List or Str
toList expects Array, Str, or List
values are not comparable
values expects Dict
|!> expected a Result while continuing a wrapped pipeline
|?> expected an Option while continuing a wrapped pipeline
```

## 6. Broad host catches

§6.9 requires every broad catch to begin with `if ($isInvariantViolation(error)) throw error;` before translating into a boundary value. The sentinel does not exist in the baseline, so every site is currently unguarded. C2 drives the unguarded count to zero.

Total: **18** — guarded: **0**, unguarded: **18**

| File | Line | Site | Sentinel rethrow |
|---|---|---|---|
| `host/browser.js` | 68 | `} catch (error) {` | **no** |
| `host/core.js` | 421 | `} catch (_) {` | **no** |
| `host/core.js` | 1570 | `} catch (_) {` | **no** |
| `host/core.js` | 1579 | `} catch (_) {` | **no** |
| `host/core.js` | 1678 | `} catch (error) {` | **no** |
| `host/core.js` | 1701 | `} catch (error) {` | **no** |
| `host/core.js` | 1765 | `} catch (error) {` | **no** |
| `host/core.js` | 1769 | `} catch (error) {` | **no** |
| `host/core.js` | 1787 | `} catch (error) {` | **no** |
| `host/core.js` | 1793 | `Promise.resolve(completion).catch(function timerActionRejected(error) {` | **no** |
| `host/core.js` | 1822 | `} catch (error) {` | **no** |
| `host/core.js` | 1848 | `} catch (error) {` | **no** |
| `host/node.js` | 80 | `} catch (error) {` | **no** |
| `host/node.js` | 316 | `} catch (error) {` | **no** |
| `host/node.js` | 551 | `} catch (error) {` | **no** |
| `host/node.js` | 597 | `} catch (error) {` | **no** |
| `host/node.js` | 742 | `} catch (error) {` | **no** |
| `host/node.js` | 760 | `} catch (error) {` | **no** |

## 7. Native error families

§7.3 requires every reserved family to become real or be removed. *Declared* means listed in the builtin union; *registered* means present in the host schema registry; *constructed* counts actual `$nativeError(...)` call sites.

| Family | Declared | Registered | Constructed | Status |
|---|---|---|---|---|
| `NativeBufferError` | yes | yes | 0 | **reserved, unused** |
| `NativeIoError` | yes | yes | 4 | in use |
| `NativeJsonError` | yes | yes | 0 | **reserved, unused** |
| `NativeNumericError` | yes | yes | 0 | **reserved, unused** |
| `NativePlatformError` | yes | yes | 0 | **reserved, unused** |
| `NativeProcessError` | yes | yes | 0 | **reserved, unused** |
| `NativeTimerError` | yes | yes | 2 | in use |

## 8. Regression summary

Counts a later phase can assert against. Every number should fall, none should rise.

| Metric | Baseline |
|---|---|
| `TAny` code occurrences | 25 |
| `ITAny` code occurrences | 3 |
| `TUnknown` code occurrences | 77 |
| `FieldMono` code occurrences | 5 |
| `FieldCommon` code occurrences | 6 |
| Builtin signatures using `Any` | 4 |
| Pfun-level unsafe intrinsic occurrences (incl. tests/examples) | 26 |
| Host `$runtimeError` sites | 63 |
| Broad host catches without sentinel rethrow | 18 |
| Reserved-but-unused native error families | 5 |
