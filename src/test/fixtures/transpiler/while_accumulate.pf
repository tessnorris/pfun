import * from "io";

proc collectEvens(limit) {
  var results = [];
  var n = 0;
  while (n <= limit) {
    if n % 2 == 0 then results = results + [n];
    n = n + 1;
  }
  results;
}
println(__str__(collectEvens(10)));
