import * from "timer";
import * from "io";

proc main() {
	match setTimer(2147483648, proc () -> Unit { }) with
	| Ok _ -> println("unexpected success")
	| Err failure -> println(
		nativeErrorOperation(failure.message)
		++ ":"
		++ nativeErrorMessage(failure.message)
	);
}

main();
