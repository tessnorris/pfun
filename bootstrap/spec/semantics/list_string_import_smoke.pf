import * from "list";
import * from "string";
import * from "io";

proc main() {
	println(__str__(sum([1, 2, 3])));
	println(join(sort([3, 1, 2]), ","));
	println(trim("  hello  "));
	println(padLeft("7", 3, "0"));
}

main();
